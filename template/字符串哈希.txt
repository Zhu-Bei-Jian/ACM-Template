 #include<cstdio>
#include<cstring>
#include<iostream>
#include<algorithm>
#include<cmath>
#include<string>
#include<map>
#include<queue>
#include<set>
#include<stack>
#include<vector>
#include<unordered_map>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;
#define in(a) a=read()
#define rep( i , a , b ) for ( int i = a ; i < b ; ++ i )
#define CLR(a) memset(a,0,sizeof a)
#define repf( i , a , b ) for ( int i = a ; i <= b ; ++ i )
const ll MAXN = 1e5+5;
const ll INF = 0x3f3f3f3f;
inline ll read() {
	ll x = 0, t = 1, c;
	while (!isdigit(c = getchar())) if (c == '-') t = -1;
	while (isdigit(c)) x = x * 10 + c - '0', c = getchar();
	return x * t;
}
 const int x=123;
 ull H[MAXN],xp[MAXN];
unordered_map<ull,ll>MAP;
ll pos;
ll n;
void riven(ll L)
{
	ll ans=0;
	rep(i,0,n-L+1)
	{ 
		ll hash_value=H[i]-H[i+L]*xp[L];
        MAP[hash_value]++;
		if(ans<MAP[hash_value])
		{
			ans=MAP[hash_value];
		}
	}

	printf("%lld\n",ans);
}
char s[MAXN];
int main()
{
	ll k;
	 in(n);
	 in(k);
    scanf("%s",s);
     H[n]=0;
    for(int i=n-1;i>=0;i--)
    {
       H[i]=H[i+1]*x+(s[i]-'a');
    }
	xp[0]=1;
	for(int i=1;i<=n;i++)xp[i]=xp[i-1]*x;

	riven(k);









#ifndef ONLINE_JUDGE
system("pause");
#endif
	return 0;
}